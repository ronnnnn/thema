name: thema
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.6.0-0 <4.0.0"

dependencies:
  collection: ^1.18.0
  macros: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
